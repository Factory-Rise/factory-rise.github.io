language: node_js

node_js:
  - '10.0.0'
cache: npm
script:
  - npm run build
deploy:
  provider: pages
  cleanup: false
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: master
  on:
    branch: development
