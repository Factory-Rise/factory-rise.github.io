language: node_js

node_js:
  - '10.0.0'
cache:
  npm: false
script:
  - npm i -g npm@6.14.8
  - npm run build
deploy:
  provider: pages
  local_dir: dist
  skip_cleanup: false
  github_token: $GITHUB_TOKEN
  keep_history: true
  target_branch: master
  on:
    branch: development
